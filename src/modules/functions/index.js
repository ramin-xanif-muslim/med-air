import { correctBreadcrumbItems } from './correctBreadcrumbItems'

export { correctBreadcrumbItems }

export function capitalizeFirstLetter(string) {
    return string[0].toUpperCase() + string.slice(1);
}